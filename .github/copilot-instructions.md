# Repository context

Read `AGENTS.md` at the repository root for shared instructions and task routing.
This repository contains starter designs, not a runnable production application.
Use `design.md` for UI work and `docs/adr/README.md` for architecture decisions.
Edit canonical skills under `skills/`, run `npm run skills:sync`, then `npm run check`.
